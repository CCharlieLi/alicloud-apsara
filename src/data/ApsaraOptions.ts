export interface AsparaOptions {
  baseUrl?: string
  timeout?: number
  version?: string
  signatureMethod?: string
  signatureVersion?: string
  accessKeyId: string
  accessKeySecret: string
  format?: string
}
